# Architecture

> Auto-generated by /map on 2026-02-19

## Overview

Taste Engine is a monorepo project structured to provide a core engine for "Top-k identification from noisy pairwise confidence" and a set of adapters.

```
┌─────────────────────────────────────────┐
│        @taste-engine/adapters           │
│      (Integration Helper Layer)         │
├─────────────────────────────────────────┤
│          @taste-engine/core             │
│    (Core Logic: Engine, Confidence)     │
└─────────────────────────────────────────┘
```

## Structure

The project uses a standard monorepo structure with `packages/`.

### @taste-engine/core
**Location:** `packages/core`
**Purpose:** Core algorithmic engine for pairwise comparison and ranking.

**Key Components:**
- **Engine** (`src/engine/`): Main entry point for the ranking logic.
- **Confidence** (`src/confidence/`): Logic for handling noisy pairwise confidence.
- **Stopping** (`src/stopping/`): Criteria for stopping the ranking process.
- **Utils** (`src/utils/`): Shared utilities.
- **Types** (`src/types.ts`): Shared type definitions (RunConfig, EngineStatus, etc.).
- **Defaults** (`src/defaults.ts`): Default configurations.

### @taste-engine/adapters
**Location:** `packages/adapters`
**Purpose:** Adapter helpers for Taste Engine (Currently in early stage).
**Components:**
- **Index** (`src/index.ts`): Basic exports/placeholder.

## Data Flow

1. **Input:** The `Engine` is initialized with a `RunConfig`.
2. **Process:**
   - The engine manages items and users.
   - It facilitates pairwise comparisons.
   - It uses `Confidence` logic to update rankings based on noisy data.
   - It checks `Stopping` criteria to determine when to finish.
3. **Output:** A ranked list of items (Top-k).

## Technical Debt & Observations

- **Adapters Package:** `packages/adapters` appears to be a skeleton.
- **Testing:** `packages/core` has a `test` directory with `engine.test.ts`, using Vitest.
- **Dependencies:** Minimal runtime dependencies. Relies heavily on internal logic.

## Conventions

- **Language:** TypeScript
- **Testing:** Vitest
- **Build:** `tsc` (TypeScript Compiler)
- **Repo:** Monorepo
