---
phase: 3
plan: 2
wave: 1
---

# Plan 3.2: Release Examples (Node & React)

## Objective
Provide high-quality example applications to demonstrate the engine's capabilities in both CLI and Web environments.

## Context
- .gsd/SPEC.md
- examples/node-cli
- packages/core

## Tasks

<task type="auto">
  <name>Refine Node CLI Example</name>
  <files>
    - examples/node-cli/index.ts
    - examples/node-cli/package.json
  </files>
  <action>
    - Ensure the Node CLI example uses the local `@taste-engine/core` via workspace linking.
    - Implement a simple interactive loop (Inquirer.js or raw readline) to vote on items.
    - Display engine status (stability, cycles) after each vote.
  </action>
  <verify>npm run start --prefix examples/node-cli</verify>
  <done>Node CLI example is interactive and correctly demonstrates engine features.</done>
</task>

<task type="auto">
  <name>Create React Web Example</name>
  <files>
    - [NEW] examples/react-web
  </files>
  <action>
    - Bootstrap a minimal Vite + React + TypeScript app in `examples/react-web`.
    - Implement a "Pairwise Voter" UI.
    - Show real-time ranking and confidence bars.
    - Use local workspace dependency for `@taste-engine/core`.
  </action>
  <verify>npm run build --prefix examples/react-web</verify>
  <done>React example builds successfully and demonstrates the web integration.</done>
</task>

## Success Criteria
- [ ] Interactive Node CLI example functioning.
- [ ] React/Vite example building and rendering.
