---
phase: 6
plan: 1
wave: 1
---

# Plan 6.1: The Session Controller (Product Brain)

## Objective
Implement the `SessionController` to decouple the UI from the raw Engine and provide an opinionated "Product Logic" layer.

## Context
- .gsd/SPEC.md
- examples/mobile-app/src/services/engineManager.ts
- .gsd/phases/6/RESEARCH.md

## Tasks

<task type="auto">
  <name>Implement SessionController Core</name>
  <files>examples/mobile-app/src/services/sessionController.ts</files>
  <action>
    - Create a `SessionController` class that manages an `Engine` instance.
    - Implement a "Dual-Buffer" for pairs: `activePair` (currently voting) and `stagedPair` (next up).
    - Add `ingest(strength)` method that triggers `Engine.ingest` and immediately promotes `stagedPair` while background-calculating the next one.
  </action>
  <verify>ls examples/mobile-app/src/services/sessionController.ts</verify>
  <done>
    - Controller handles state transisitons and pair buffering.
  </done>
</task>

<task type="auto">
  <name>Implement Experience Logic</name>
  <files>examples/mobile-app/src/services/sessionController.ts</files>
  <action>
    - Add `getStatusMessage()` with dynamic thresholds (e.g. "Almost there!" if stability > 80%).
    - Implement "Best Current Answer" logic for early exports if `Engine.status().canStop` is true.
    - Integrate auto-saving via `engineManager` after every vote.
  </action>
  <verify>grep "getStatusMessage" examples/mobile-app/src/services/sessionController.ts</verify>
  <done>
    - UX messaging and auto-save are operational.
  </done>
</task>

## Success Criteria
- [ ] UI can query the controller for a "ready-to-go" pair with no wait time.
- [ ] Controller provides human-readable status strings based on math.
