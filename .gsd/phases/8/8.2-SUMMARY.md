---
phase: 8
plan: 2
status: complete
---

# Summary 8.2: Guardrail Helpers — Cooldowns & Seen Sets

## What Was Done
- Created `src/redis/cooldown.ts`:
  - `isOnCooldown(userId)` — checks `cooldown:{userId}` key existence
  - `recordVote(userId)` — INCR rate counter (10s window), triggers cooldown if >30 votes
  - `triggerCooldown(userId)` — sets `cooldown:{userId}` with 300s TTL
- Created `src/redis/seen.ts`:
  - `hasSeen(userId, contestId, pk)` — ZSCORE membership check
  - `markSeen(userId, contestId, pk)` — ZADD with Date.now() score, trims to 5000-entry cap via ZREMRANGEBYRANK

## Verification
- `cooldown.ts` exports `isOnCooldown`, `recordVote`, `triggerCooldown` ✓
- `seen.ts` exports `hasSeen` and `markSeen` ✓
- Both import only from `./client` ✓
- Key schemas match RESEARCH.md ✓
- Seen set capped at 5000 entries ✓
