---
phase: 5
plan: 6
wave: 2
---

# Plan 5.6: Mobile MVP Polish (User Loop)

## Objective
Finalize the mobile user experience by adding playlist selection, target K setting, and real track metadata display.

## Context
- examples/mobile-app/src/services/spotify.ts
- examples/mobile-app/src/screens/VotingScreen.tsx

## Tasks

<task type="auto">
  <name>Implement Metadata Cache</name>
  <files>examples/mobile-app/src/services/engineManager.ts</files>
  <action>
    - Update `EngineManager` to store a mapping of `ItemId -> { name, artist, imageUrl }`.
    - Persians this cache to MMKV so track info is available immediately without API calls on resume.
  </action>
  <verify>grep "metadata" examples/mobile-app/src/services/engineManager.ts</verify>
  <done>
    - Metadata cache implemented and persisted.
  </done>
</task>

<task type="auto">
  <name>Build Playlist Selection Screen</name>
  <files>examples/mobile-app/src/screens/PlaylistPicker.tsx</files>
  <action>
    - Create a screen that fetches user playlists via `SpotifyService.getPlaylists()`.
    - On selection, prompt for "Target K" (10, 20, 50, 100).
    - Initialize the engine with the picked playlist tracks and navigation to `VotingScreen`.
  </action>
  <verify>ls examples/mobile-app/src/screens/PlaylistPicker.tsx</verify>
  <done>
    - Users can now pick a real playlist to start.
  </done>
</task>

<task type="auto">
  <name>Finalize Voting Screen</name>
  <files>examples/mobile-app/src/screens/VotingScreen.tsx</files>
  <action>
    - Replace "Loading..." placeholders with real track names from the `EngineManager` metadata cache.
    - Implement a "Restart" button that returns to the `PlaylistPicker`.
  </action>
  <verify>grep -v "Loading..." examples/mobile-app/src/screens/VotingScreen.tsx</verify>
  <done>
    - UI shows real music data.
  </done>
</task>

## Success Criteria
- [ ] User can go from Login -> Pick Playlist -> Set K -> Vote (with real names) -> Export.
