---
phase: 5
plan: 1
wave: 1
---

# Plan 5.1: Mobile Foundation

## Objective
Initialize the Expo project and configure the secure storage infrastructure required for auth and state.

## Context
- .gsd/SPEC.md
- .gsd/phases/5/RESEARCH.md

## Tasks

<task type="auto">
  <name>Initialize Expo App</name>
  <files>examples/mobile-app/</files>
  <action>
    - Create a new Expo project in `examples/mobile-app` using the `tabs` template (or blank template with TypeScript).
    - Configure `package.json` to include dependencies identified in research: `expo-router`, `expo-secure-store`, `react-native-mmkv`, `react-native-reanimated`, `@react-native-community/slider`, `expo-haptics`.
    - Set up the workspace link to `@taste-engine/core`.
  </action>
  <verify>npm list --prefix examples/mobile-app</verify>
  <done>
    - Project initialized in `examples/mobile-app`.
    - Dependencies installed and workspace link functional.
  </done>
</task>

<task type="auto">
  <name>Configure Secure Storage Service</name>
  <files>examples/mobile-app/src/services/storage.ts</files>
  <action>
    - Implement a `StorageService` that wraps `react-native-mmkv` for fast JSON (engine snapshots).
    - Implement an `AuthStorage` service that wraps `expo-secure-store` for sensitive tokens.
  </action>
  <verify>ls examples/mobile-app/src/services/storage.ts</verify>
  <done>
    - Storage services created and exported.
  </done>
</task>

## Success Criteria
- [ ] Expo project successfully boots (simulated via file existence).
- [ ] Storage abstraction layer is ready for Auth and Engine.
