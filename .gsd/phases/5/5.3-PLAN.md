---
phase: 5
plan: 3
wave: 1
---

# Plan 5.3: Engine Adapter & State Persistence

## Objective
Connect the core `@taste-engine/core` library to the mobile persistence layer.

## Context
- .gsd/SPEC.md
- packages/core/src/engine/engine.ts
- examples/mobile-app/src/services/storage.ts

## Tasks

<task type="auto">
  <name>Create Mobile Engine Wrapper</name>
  <files>examples/mobile-app/src/services/engineManager.ts</files>
  <action>
    - Create a manager that initializes a Taste Engine instance for a given Spotify Playlist ID.
    - Implement `autoSave` logic that writes an `EngineSnapshot` to MMKV after every few votes.
    - Implement `resumeSession(playlistId)` to reload state from MMKV.
  </action>
  <verify>ls examples/mobile-app/src/services/engineManager.ts</verify>
  <done>
    - Wrapper provides `ingest`, `save`, and `load` methods tied to mobile storage.
  </done>
</task>

<task type="auto">
  <name>Implement Stability Metrics Hook</name>
  <files>examples/mobile-app/src/hooks/useEngineStatus.ts</files>
  <action>
    - Create a React hook that subscribes to engine updates and exposes `stability %` and `messaging` (e.g., "Almost there").
    - Use common thresholds (80%, 90%) for the UI strings.
  </action>
  <verify>ls examples/mobile-app/src/hooks/useEngineStatus.ts</verify>
  <done>
    - Hook provides reactive status data for the UI.
  </done>
</task>

## Success Criteria
- [ ] Engine state survives app restarts.
- [ ] UI has access to real-time confidence metrics.
