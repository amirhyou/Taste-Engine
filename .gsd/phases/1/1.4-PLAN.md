---
phase: 1
plan: 1.4
wave: 2
---

# Plan 1.4: Lifecycle, Stopping & API

## Objective
Finalize the Core Engine by implementing stopping rules, snapshotting (import/export), and exposing the public API.

## Context
- src/engine/index.ts
- src/confidence/index.ts
- .gsd/ROADMAP.md

## Tasks

<task type="auto">
  <name>Implement Stopping Rules</name>
  <files>src/stopping/index.ts, src/engine/index.ts</files>
  <action>
    Implement `shouldStop(status: EngineStatus, config: RunConfig) -> boolean`.
    - Criteria:
        1. `minComparisons` reached.
        2. `stability` > `status.targetStability`.
    - Integrate into `Engine.getStatus()`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Engine reports `done: true` when conditions met.</done>
</task>

<task type="auto">
  <name>Implement Lifecycle (Import/Export)</name>
  <files>src/engine/index.ts, test/engine/lifecycle.test.ts</files>
  <action>
    Add methods to `TasteEngine` for state persistence.
    - `exportState()`: Returns JSON-serializable object (items, total comparisons, config).
    - `importState(json)`: Rehydrates the engine.
    - Ensure RNG state is also serialized if possible (Mulberry32 state), or documented that it resets.
  </action>
  <verify>npx vitest run lifecycle</verify>
  <done>State can be saved and reloaded exactly.</done>
</task>

<task type="auto">
  <name>Finalize Public API</name>
  <files>src/index.ts</files>
  <action>
    Export the main public interface in `packages/core/src/index.ts`.
    - `TasteEngine` class.
    - `RunConfig`, `Item`, `EngineStatus` types.
    - (Optional) `createEngine` factory.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Package exports are clean.</done>
</task>

## Success Criteria
- [ ] Stopping logic works.
- [ ] Full import/export cycle works.
- [ ] library `index.ts` is ready for consumption.
