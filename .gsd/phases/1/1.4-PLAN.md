---
phase: 1
plan: 1.4
wave: 2
---

# Plan 1.4: Lifecycle, Stopping & API (Review & Finalize)

## Objective
Finalize the Core Engine by verifying stopping rules, snapshotting, and the public API export.

## Context
- src/stopping/stopping.ts (Existing)
- src/engine/engine.ts (Existing snapshot logic)
- src/index.ts (Entry point)

## Tasks

<task type="auto">
  <name>Verify Stopping Rules</name>
  <files>src/stopping/stopping.ts</files>
  <action>
    Review `src/stopping/stopping.ts`.
    - Ensure it correctly interprets `pInTopK` from the confidence module.
    - Verify `shouldStop` logic matches the specific criteria in `SPEC.md` (Top-K stability).
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Stopping logic is robust.</done>
</task>

<task type="auto">
  <name>Verify Lifecycle (Snapshot/Load)</name>
  <files>src/engine/engine.ts, test/engine/lifecycle.test.ts</files>
  <action>
    Review `snapshot()` and `loadSnapshot()` in `Engine`.
    - Ensure all state (including RNG state if possible, though maybe difficult with closures) is handled.
    - Ensure `RunConfig` is correctly serialized/deserialized.
    - Create a test case: Run engine -> Snapshot -> New Engine -> Load -> Verify state matches.
  </action>
  <verify>npx vitest run lifecycle</verify>
  <done>Import/Export doesn't lose data.</done>
</task>

<task type="auto">
  <name>Finalize Public API</name>
  <files>src/index.ts</files>
  <action>
    Ensure `packages/core/src/index.ts` exports everything needed for consumers.
    - `Engine`, `RunConfig`, `Item`, `EngineStatus`.
    - Check for any internal types leaking or missing exports.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Public API is clean.</done>
</task>

## Success Criteria
- [ ] Stopping logic verified.
- [ ] Snapshotting verified.
- [ ] Public API verified.
