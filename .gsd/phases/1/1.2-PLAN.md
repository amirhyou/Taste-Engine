---
phase: 1
plan: 1.2
wave: 1
---

# Plan 1.2: Core Model (Refactor to Gaussian updates)

## Objective
Upgrade the existing `OnlineModel` to use rigorous Gaussian update equations (Thurstone-Mosteller) instead of the current heuristic approximations.

## Context
- src/model/onlineModel.ts (Existing heuristic model)
- src/utils/statistics.ts (New stats utils)
- .gsd/phases/1/RESEARCH.md

## Tasks

<task type="auto">
  <name>Refactor OnlineModel to Gaussian Logic</name>
  <files>src/model/onlineModel.ts, test/model/gaussian.test.ts</files>
  <action>
    Refactor `src/model/onlineModel.ts`:
    - Replace the heuristic `ingest` logic with the correct Gaussian update equations.
    - Use `pdf(margin)`, `cdf(margin)` to calculate `v` (signal) and `w` (variance).
    - Update `mu` and `sigma` using `v` and `w`.
    - Ensure `predict` uses `cdf` of the difference (probit) instead of logistic function, OR clearly document the choice (TrueSkill uses probit).
    
    Update/Create tests to verify direction of updates and convergence.
  </action>
  <verify>npx vitest run gaussian</verify>
  <done>Model implementation uses stats utils and passes verification tests.</done>
</task>

<task type="auto">
  <name>Review Engine Integration</name>
  <files>src/engine/engine.ts, test/engine/core.test.ts</files>
  <action>
    Review `src/engine/engine.ts`.
    - Ensure `ingest` calls `model.ingest` correctly.
    - Verify `nextPair` passes correct context to selector.
    - Add integration tests if missing to cover the full loop.
  </action>
  <verify>npx vitest run core</verify>
  <done>Engine works with the new Gaussian model.</done>
</task>

## Success Criteria
- [ ] `ingest` uses formal Gaussian update logic.
- [ ] `predict` uses appropriate probability function.
- [ ] Existing tests pass (or are updated to reflect new math).
