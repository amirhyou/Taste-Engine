---
phase: 5
plan: 2
wave: 1
---

# Plan 5.2: Spotify Auth (PKCE)

## Objective
Implement the secure Spotify login flow using PKCE for mobile users.

## Context
- .gsd/SPEC.md
- .gsd/phases/5/RESEARCH.md
- examples/mobile-app/src/services/storage.ts

## Tasks

<task type="auto">
  <name>Implement Spotify Auth Service</name>
  <files>examples/mobile-app/src/services/spotifyAuth.ts</files>
  <action>
    - Use `expo-auth-session` to implement the `useSpotifyAuth` hook.
    - Configure PKCE (Code Challenge/Verifier).
    - Implement token refresh logic using the `AuthStorage` from Plan 5.1.
  </action>
  <verify>ls examples/mobile-app/src/services/spotifyAuth.ts</verify>
  <done>
    - Auth service successfully implements PKCE.
    - Automatic token refresh logic is in place.
  </done>
</task>

<task type="checkpoint:decision">
  <name>Spotify Redirect URI confirmation</name>
  <files>app.json</files>
  <action>
    - User needs to confirm the Custom Scheme (e.g. `exp+taste-engine`) to be used for Spotify redirects in the Developer Dashboard.
  </action>
  <verify>grep "scheme" examples/mobile-app/app.json</verify>
  <done>
    - Scheme is configured in `app.json`.
  </done>
</task>

## Success Criteria
- [ ] Auth service ready to handle one-tap login and persistent sessions.
