---
phase: 5
plan: 4
wave: 2
---

# Plan 5.4: Voting Screen (Vertical Slider UI)

## Objective
Implement the high-precision mobile voting interface with vertical pair stacking and strength-of-preference slider.

## Context
- .gsd/SPEC.md
- .gsd/phases/5/RESEARCH.md

## Tasks

<task type="auto">
  <name>Build Vertical Pair Layout</name>
  <files>examples/mobile-app/src/components/PairStack.tsx</files>
  <action>
    - Create a component that renders two cards: Item A (Top) and Item B (Bottom).
    - Use `react-native-reanimated` for smooth entrance/exit transitions between pairs.
  </action>
  <verify>ls examples/mobile-app/src/components/PairStack.tsx</verify>
  <done>
    - Layout correctly positions two items vertically for ergonomic thumb usage.
  </done>
</task>

<task type="auto">
  <name>Implement Strength Slider with Haptics</name>
  <files>examples/mobile-app/src/components/StrengthSlider.tsx</files>
  <action>
    - Implement a horizontal slider (±1.0 range).
    - Integrate `expo-haptics` to vibrate when passing the 0.0 center point.
    - Link slider `onValueChange` to the engine's `ingest` via the `engineManager`.
  </action>
  <verify>ls examples/mobile-app/src/components/StrengthSlider.tsx</verify>
  <done>
    - Slider provides tactile feedback and feeds votes into the engine.
  </done>
</task>

## Success Criteria
- [ ] Users can vote using tactile slider.
- [ ] UI clearly indicates which item is currently favored.
