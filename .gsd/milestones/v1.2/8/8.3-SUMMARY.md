---
phase: 8
plan: 3
status: complete
---

# Summary 8.3: RedisDispatcher & Route Wiring

## What Was Done
- Created `src/dispatch/pairKey.ts` — `pairKey(a, b)` formula (lexicographic, `a::b`)
- Created `src/dispatch/RedisDispatcher.ts`:
  - `getNextPair(contestId, userId)` — cooldown check → up to 5 seen-set filtered nextPair retries → markSeen
  - `submitVote(contestId, userId, vote)` — cooldown check → lock (3 retries, 50ms) → coordinator.submitVote → releaseLock (finally) → recordVote → markSeen
  - `HttpError` class with `.status` for 409/429
- Updated `src/api/routes.ts`:
  - Fixed `zValidator(schema)` → `zValidator('json', schema)` and `zValidator('query', schema)`
  - `userId` now required (`z.string()`) in `VoteSchema`
  - `NextQuerySchema` added — `GET /next` now requires `?userId=`
  - Routes use `dispatcher` (not coordinator directly); 409/429 caught and returned as JSON
  - `coordinator` still exported for `index.ts` snapshotAll compatibility

## Verification
- All `zValidator` calls use target string ✓
- `userId` required in vote and next endpoints ✓
- Lock released in `finally` block ✓
- 409 on lock failure, 429 on cooldown ✓
- `pairKey` defined locally (not imported from core) ✓
