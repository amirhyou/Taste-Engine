---
phase: 8
plan: 1
status: complete
---

# Summary 8.1: Redis Infrastructure — Shared Client & Pair Locks

## What Was Done
- Created `src/redis/client.ts` — single IORedis instance reading `REDIS_URL` env (fallback `redis://localhost:6379`), `maxRetriesPerRequest: null` for BullMQ compatibility
- Created `src/redis/locks.ts` — `acquireLock` (SET NX PX 60000) and `releaseLock` (Lua safe-delete)
- Updated `src/worker/bullmq.ts` — removed inline `new IORedis()`, now imports shared `redis` from `../redis/client`

## Verification
- `client.ts` exports `redis` with REDIS_URL config ✓
- `locks.ts` exports `acquireLock` and `releaseLock` ✓
- `bullmq.ts` no longer imports IORedis directly ✓
- Lua script ensures lock is only deleted by the owner token ✓

## Notes
- Pre-existing TS errors in bullmq.ts (cannot find module 'bullmq', implicit any) — caused by missing npm install in local env, not this change
