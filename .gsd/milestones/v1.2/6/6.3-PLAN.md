---
phase: 6
plan: 3
wave: 2
---

# Plan 6.3: Durable Session Management

## Objective
Evolve local storage into a robust session manager with history, archival, and cleanup.

## Context
- examples/mobile-app/src/services/storage.ts
- examples/mobile-app/src/services/engineManager.ts

## Tasks

<task type="auto">
  <name>Implement Session Index</name>
  <files>examples/mobile-app/src/services/sessionManager.ts</files>
  <action>
    - Create a `SessionManager` that maintains a central `index` of all local sessions.
    - Track session metadata: `playlistName`, `lastActive`, `status` (active/archived), and `targetK`.
  </action>
  <verify>ls examples/mobile-app/src/services/sessionManager.ts</verify>
  <done>
    - Meta-storage for multiple sessions is operational.
  </done>
</task>

<task type="auto">
  <name>Cleanup & Integrity</name>
  <files>examples/mobile-app/src/services/sessionManager.ts</files>
  <action>
    - Add `pruneSessions()` logic to delete old snapshots if storage exceeds a limit.
    - Implement version checking: refuse to load snapshots from older CORE versions if schema changed.
  </action>
  <verify>grep "prune" examples/mobile-app/src/services/sessionManager.ts</verify>
  <done>
    - Storage stays clean and incompatible snapshots are handled gracefully.
  </done>
</task>

## Success Criteria
- [ ] Users can see a list of past "Taste Journeys" (future UI hook provided).
- [ ] App doesn't grow indefinitely in storage.
