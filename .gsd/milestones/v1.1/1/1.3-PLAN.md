---
phase: 1
plan: 1.3
wave: 2
---

# Plan 1.3: Selection & Confidence (Review & Refactor)

## Objective
Refactor and verify the `Selector` and `Confidence` modules. Ensure `BoundarySelector` aligns with the "Smart Defaults" goal and `Confidence` uses a robust Monte Carlo approach.

## Context
- src/selector/selector.ts (Existing)
- src/confidence/confidence.ts (Existing)
- src/engine/engine.ts

## Tasks

<task type="auto">
  <name>Refine BoundarySelector</name>
  <files>src/selector/selector.ts</files>
  <action>
    Review `src/selector/selector.ts`.
    - Verify the logic for `boundaryBand` and `pool` selection.
    - Ensure `pickAnchors` logic in `Engine` (or Selector) is sound for onboarding.
    - Refactor `pairScore` if needed to better balance information gain vs. boundary exploration.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Selector provides optimal pairs for the Gaussian model.</done>
</task>

<task type="auto">
  <name>Refactor Confidence Module</name>
  <files>src/confidence/confidence.ts, test/confidence/montecarlo.test.ts</files>
  <action>
    Review `src/confidence/confidence.ts`.
    - Verify `computeConfidence` implements Monte Carlo sampling correctly.
    - Ensure it uses `model.sampleScores` (which uses `boxMuller` in `onlineModel.ts`).
    - Standardize the `stability` metric (Kendall's tau or Top-K intersection).
  </action>
  <verify>npx vitest run montecarlo</verify>
  <done>Confidence computation is statistically valid.</done>
</task>

## Success Criteria
- [ ] `BoundarySelector` logic confirmed/improved.
- [ ] `Confidence` module confirmed/improved.
- [ ] Integration with `Engine` verified.
